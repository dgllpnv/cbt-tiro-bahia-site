# =====================================================
# CBT — Frontend (.env.example)
# Copie este arquivo para `.env` antes de rodar.
# O script start-dev.ps1 / start-dev.sh cria automaticamente
# se ele não existir.
# =====================================================

# URL da API backend. Em produção troque para o domínio público.
VITE_API_URL=http://localhost:3002
